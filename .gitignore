# Dependencies
node_modules/

# Next.js build output
.next/
out/
build/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Local env (Next convention — not committed)
.env.local
.env*.local

# OS / editor junk
.DS_Store
Thumbs.db
*.swp

# Logs
npm-debug.log*
yarn-error.log*
uvicorn.log

# TypeScript incremental build cache
*.tsbuildinfo

# Dev artefacts (captures Playwright, scripts de smoke) : jamais commites
frontend/shots/

# Local env — never committed (public repo; backend/.env carries the API key)
.env
